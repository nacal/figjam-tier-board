{
  "name": "figjam-tier",
  "version": "0.1.0",
  "private": true,
  "description": "FigJam 上で付箋を並べる Tier 表の行（ティア）を管理するプラグイン",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json --watch",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.100.2",
    "typescript": "^5.6.3"
  }
}
